# C Practice

A repository for C programming exercises, algorithms, and practice projects.

## Project Structure
- `.gitignore`: Configured to ignore binary files, build artifacts, and object files.
- Add your `.c` source files and headers here.

## Compilation Example
```bash
gcc -Wall -Wextra -o main main.c
./main
```
